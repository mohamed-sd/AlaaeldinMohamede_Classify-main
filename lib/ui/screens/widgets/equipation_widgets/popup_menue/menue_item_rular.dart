import 'package:flutter/material.dart';

class Rular extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      height: 1,
      color: Colors.black,
      width: double.infinity,
    );
  }
}
